@model guia_turistico.Models.Tipos
@{
    ViewData["Title"] = "Editar Tipo de Sitio Turístico";
}

<div class="container py-4">
    <!-- Header con navegación -->
    <div class="bg-primary-gradient text-white py-4">
        <div class="container">
            <div class="d-flex flex-column flex-md-row justify-content-between align-items-center gap-3">
                <div class="text-center text-md-start">
                    <h1 class="display-6 fw-bold mb-1">
                        <i class="fas fa-edit me-2"></i>Editar Tipo de Sitio Turístico
                    </h1>
                    <p class="lead mb-0 opacity-75">Modifica la información de esta categoría de sitios turísticos</p>
                </div>
                <a asp-action="Index" class="btn btn-outline-light btn-lg">
                    <i class="fas fa-arrow-left me-2"></i>Volver al Listado
                </a>
            </div>
        </div>
    </div>

    <!-- Formulario Principal -->
    <div class="container py-4">
        <form asp-action="Edit" id="typeForm" class="needs-validation" novalidate>
            @Html.AntiForgeryToken()
            <div asp-validation-summary="ModelOnly" class="alert alert-danger" role="alert"></div>
            <input type="hidden" asp-for="TipoId" />

            <!-- Sección de Imagen -->
            <div class="card shadow-lg mb-5 border-0 overflow-hidden">
                <div class="card-header bg-primary bg-gradient py-3 text-white">
                    <h3 class="h5 mb-0">
                        <i class="fas fa-image me-2"></i>Imagen Representativa
                    </h3>
                </div>
                <div class="card-body p-0">
                    <div class="row g-0">
                        <!-- URL de Imagen -->
                        <div class="col-lg-8">
                            <div class="p-4">
                                <h5 class="fw-semibold mb-3">
                                    <i class="fas fa-link me-2 text-primary"></i>URL de la Imagen
                                </h5>

                                <div class="mb-4">
                                    <div class="form-group">
                                        <label asp-for="ImagenUrl" class="form-label fw-semibold">
                                            URL de Imagen
                                        </label>
                                        <div class="input-group">
                                            <span class="input-group-text">
                                                <i class="fas fa-link"></i>
                                            </span>
                                            <input asp-for="ImagenUrl"
                                                   class="form-control"
                                                   placeholder="Ej: https://ejemplo.com/imagen-tipo.jpg"
                                                   id="imageUrlInput"
                                                   type="url"
                                                   data-bs-toggle="tooltip"
                                                   data-bs-title="Ejemplo: https://ejemplo.com/imagen.jpg (JPG, PNG, etc.)">
                                        </div>
                                        <span asp-validation-for="ImagenUrl" class="invalid-feedback d-block"></span>
                                    </div>

                                    <!-- Feedback de validación Bootstrap -->
                                    <div class="valid-feedback" id="urlValidFeedback">
                                        <i class="fas fa-check-circle me-1"></i>URL de imagen válida
                                    </div>
                                    <div class="invalid-feedback" id="urlInvalidFeedback">
                                        <i class="fas fa-exclamation-circle me-1"></i>No se pudo cargar la imagen
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- Panel de Vista Previa -->
                        <div class="col-lg-4 border-start bg-light">
                            <div class="p-4">
                                <h5 class="fw-semibold mb-3">
                                    <i class="fas fa-eye me-2 text-primary"></i>Vista Previa
                                </h5>

                                <div class="border rounded-3 p-3 bg-white">
                                    <h6 class="fw-semibold mb-2 text-dark">
                                        <i class="fas fa-image me-2 text-success"></i>Imagen del Tipo
                                    </h6>

                                    <!-- Vista previa de imagen -->
                                    <div class="ratio ratio-1x1 mb-3">
                                        <img src="@(!string.IsNullOrEmpty(Model.ImagenUrl) ? Model.ImagenUrl : "data:image/svg+xml;base64,")"
                                             class="img-fluid rounded shadow-sm object-fit-cover"
                                             id="imagePreview"
                                             alt="Vista previa de la imagen">
                                    </div>

                                    <!-- Indicador de estado -->
                                    <div class="d-flex align-items-center">
                                        <span class="badge bg-success px-3 py-2 me-2" id="imageStatus">
                                            <i class="fas fa-check-circle me-1"></i>Imagen cargada
                                        </span>
                                        <span class="badge bg-info px-3 py-2" id="originalImageBadge" style="display: none;">
                                            <i class="fas fa-history me-1"></i>Imagen modificada
                                        </span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Información Principal del Tipo -->
            <div class="row g-4 mb-4">
                <!-- Columna Izquierda - Información en Español -->
                <div class="col-lg-6">
                    <div class="card h-100 border-0 shadow-sm">
                        <div class="card-header border-bottom py-3">
                            <h4 class="h5 mb-0">
                                <i class="fas fa-flag me-2"></i>Información en Español
                            </h4>
                        </div>
                        <div class="card-body">
                            <div class="mb-4">
                                <label asp-for="Nombre" class="form-label fw-semibold">
                                    <i class="fas fa-signature me-2 text-muted"></i>Nombre del Tipo *
                                </label>
                                <input asp-for="Nombre"
                                       class="form-control"
                                       placeholder="Ej: Playa, Museo, Parque Nacional"
                                       required
                                       maxlength="50"
                                       data-bs-toggle="tooltip"
                                       data-bs-title="Nombre único para identificar este tipo de sitio turístico">
                                <div class="form-text text-end">
                                    <span class="text-muted" id="nombreCounter">@Model.Nombre?.Length/50</span>
                                </div>
                                <span asp-validation-for="Nombre" class="invalid-feedback"></span>
                            </div>

                            <div class="mb-0">
                                <label asp-for="Descripcion" class="form-label fw-semibold">
                                    <i class="fas fa-align-left me-2 text-muted"></i>Descripción
                                </label>
                                <textarea asp-for="Descripcion"
                                          class="form-control"
                                          rows="5"
                                          placeholder="Ej: Sitios con acceso al mar, arena y servicios playeros..."
                                          maxlength="500"
                                          data-bs-toggle="tooltip"
                                          data-bs-title="Características comunes de este tipo de sitio"></textarea>
                                <div class="form-text text-end">
                                    <span class="text-muted" id="descripcionCounter">@Model.Descripcion?.Length/500</span>
                                </div>
                                <span asp-validation-for="Descripcion" class="invalid-feedback"></span>
                            </div>
                        </div>
                    </div>
                </div>

                <!-- Columna Derecha - Traducciones -->
                <div class="col-lg-6">
                    <div class="card h-100 border-0 shadow-sm">
                        <div class="card-header border-bottom py-3">
                            <h4 class="h5 mb-0">
                                <i class="fas fa-language me-2"></i>Información Multilingüe
                            </h4>
                        </div>
                        <div class="card-body">
                            <!-- Pestañas para idiomas -->
                            <ul class="nav nav-tabs mb-4" id="langTabs" role="tablist">
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link active"
                                            id="english-tab"
                                            data-bs-toggle="tab"
                                            data-bs-target="#english"
                                            type="button"
                                            role="tab"
                                            aria-controls="english"
                                            aria-selected="true">
                                        <i class="fas fa-language me-1"></i> Inglés
                                    </button>
                                </li>
                                <li class="nav-item" role="presentation">
                                    <button class="nav-link"
                                            id="portuguese-tab"
                                            data-bs-toggle="tab"
                                            data-bs-target="#portuguese"
                                            type="button"
                                            role="tab"
                                            aria-controls="portuguese"
                                            aria-selected="false">
                                        <i class="fas fa-language me-1"></i> Portugués
                                    </button>
                                </li>
                            </ul>

                            <div class="tab-content" id="langTabsContent">
                                <!-- Inglés -->
                                <div class="tab-pane fade show active"
                                     id="english"
                                     role="tabpanel"
                                     aria-labelledby="english-tab"
                                     tabindex="0">
                                    <div class="mb-4">
                                        <label asp-for="NombreIngles" class="form-label fw-semibold">
                                            Nombre en Inglés
                                        </label>
                                        <input asp-for="NombreIngles"
                                               class="form-control"
                                               placeholder="Ej: Beach, Museum, National Park"
                                               maxlength="50"
                                               data-bs-toggle="tooltip"
                                               data-bs-title="Traducción al inglés para internacionalización">
                                        <div class="form-text text-end">
                                            <span class="text-muted" id="nombreInglesCounter">@Model.NombreIngles?.Length/50</span>
                                        </div>
                                        <span asp-validation-for="NombreIngles" class="invalid-feedback"></span>
                                    </div>

                                    <div>
                                        <label asp-for="DescripcionIngles" class="form-label fw-semibold">
                                            Descripción en Inglés
                                        </label>
                                        <textarea asp-for="DescripcionIngles"
                                                  class="form-control"
                                                  rows="4"
                                                  placeholder="Ej: Sites with sea access, sand and beach services..."
                                                  maxlength="500"
                                                  data-bs-toggle="tooltip"
                                                  data-bs-title="Descripción en inglés para visitantes extranjeros"></textarea>
                                        <div class="form-text text-end">
                                            <span class="text-muted" id="descripcionInglesCounter">@Model.DescripcionIngles?.Length/500</span>
                                        </div>
                                        <span asp-validation-for="DescripcionIngles" class="invalid-feedback"></span>
                                    </div>
                                </div>

                                <!-- Portugués -->
                                <div class="tab-pane fade"
                                     id="portuguese"
                                     role="tabpanel"
                                     aria-labelledby="portuguese-tab"
                                     tabindex="0">
                                    <div class="mb-4">
                                        <label asp-for="NombrePortugues" class="form-label fw-semibold">
                                            Nombre en Portugués
                                        </label>
                                        <input asp-for="NombrePortugues"
                                               class="form-control"
                                               placeholder="Ej: Praia, Museu, Parque Nacional"
                                               maxlength="50"
                                               data-bs-toggle="tooltip"
                                               data-bs-title="Traducción al portugués para visitantes brasileños">
                                        <div class="form-text text-end">
                                            <span class="text-muted" id="nombrePortuguesCounter">@Model.NombrePortugues?.Length/50</span>
                                        </div>
                                        <span asp-validation-for="NombrePortugues" class="invalid-feedback"></span>
                                    </div>

                                    <div>
                                        <label asp-for="DescripcionPortugues" class="form-label fw-semibold">
                                            Descripción en Portugués
                                        </label>
                                        <textarea asp-for="DescripcionPortugues"
                                                  class="form-control"
                                                  rows="4"
                                                  placeholder="Ej: Locais com acesso ao mar, areia e serviços de praia..."
                                                  maxlength="500"
                                                  data-bs-toggle="tooltip"
                                                  data-bs-title="Descripción en portugués para visitantes lusófonos"></textarea>
                                        <div class="form-text text-end">
                                            <span class="text-muted" id="descripcionPortuguesCounter">@Model.DescripcionPortugues?.Length/500</span>
                                        </div>
                                        <span asp-validation-for="DescripcionPortugues" class="invalid-feedback"></span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Acciones con Modal de Confirmación -->
            <div class="card border-0 shadow-sm">
                <div class="card-body p-4">
                    <div class="d-flex flex-column flex-md-row justify-content-between align-items-center gap-3">
                        <div class="text-center text-md-start">
                            <h5 class="fw-semibold mb-1">¿Estás listo para guardar los cambios?</h5>
                            <p class="text-muted small mb-0">
                                Verifica que toda la información sea correcta antes de actualizar
                            </p>
                        </div>

                        <div class="d-flex gap-3">
                            <button type="button" class="btn btn-outline-danger btn-lg px-5"
                                    data-bs-toggle="modal" data-bs-target="#restoreModal">
                                <i class="fas fa-undo me-2"></i>Restaurar Original
                            </button>
                            <button type="submit" class="btn btn-success btn-lg px-5" id="submitBtn">
                                <i class="fas fa-save me-2"></i>Actualizar Tipo
                            </button>
                        </div>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>

<!-- Modal de Confirmación para Restaurar -->
<div class="modal fade" id="restoreModal" tabindex="-1" aria-labelledby="restoreModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content">
            <div class="modal-header">
                <h5 class="modal-title" id="restoreModalLabel">
                    <i class="fas fa-exclamation-triangle text-warning me-2"></i>Confirmar Restauración
                </h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body">
                <p>¿Estás seguro de que deseas restaurar todos los valores originales?</p>
                <p class="text-muted small">Se perderán todos los cambios no guardados.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">
                    <i class="fas fa-times me-2"></i>Cancelar
                </button>
                <button type="button" class="btn btn-danger" id="confirmRestore">
                    <i class="fas fa-undo me-2"></i>Restaurar Todo
                </button>
            </div>
        </div>
    </div>
</div>

<!-- Toast para Mensajes -->
<div class="position-fixed bottom-0 end-0 p-3" style="z-index: 11">
    <div id="liveToast" class="toast" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="toast-header">
            <i class="fas fa-info-circle me-2 text-primary"></i>
            <strong class="me-auto">Sistema</strong>
            <small>Ahora</small>
            <button type="button" class="btn-close" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
        <div class="toast-body" id="toastMessage">
            Mensaje de ejemplo
        </div>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }

    <script>
        // Configuración global para Edit de Tipos
        const config = {
            isTestMode: window.navigator.webdriver === true ||
                       window.location.search.includes('testmode'),
            defaultImage: 'data:image/svg+xml;base64,',
            errorImage: 'data:image/svg+xml;base64,',
            originalData: {
                Nombre: "@Html.Raw(Model.Nombre?.Replace("\"", "\\\""))",
                Descripcion: "@Html.Raw(Model.Descripcion?.Replace("\"", "\\\"").Replace("\r\n", "\\n"))",
                NombreIngles: "@Html.Raw(Model.NombreIngles?.Replace("\"", "\\\""))",
                DescripcionIngles: "@Html.Raw(Model.DescripcionIngles?.Replace("\"", "\\\"").Replace("\r\n", "\\n"))",
                NombrePortugues: "@Html.Raw(Model.NombrePortugues?.Replace("\"", "\\\""))",
                DescripcionPortugues: "@Html.Raw(Model.DescripcionPortugues?.Replace("\"", "\\\"").Replace("\r\n", "\\n"))",
                ImagenUrl: "@Html.Raw(Model.ImagenUrl?.Replace("\"", "\\\""))"
            }
        };

        // Módulo para manejo del formulario en edición
        const formManager = {
            hasChanges: false,
            imageChanged: false,

            init() {
                // Configurar modo test si es necesario
                if (config.isTestMode) {
                    document.body.classList.add('test-mode');
                    console.log('✅ Modo de pruebas activado');
                }

                // Inicializar componentes básicos
                this.initBasicComponents();
                this.setupCharacterCounters();
                this.setupImagePreview();
                this.setupChangeDetection();

                // Configurar validación según modo
                if (config.isTestMode) {
                    this.setupTestFriendlyForm();
                } else {
                    this.setupUserFriendlyForm();
                }

                // Configurar modal de restauración
                this.setupRestoreModal();
            },

            initBasicComponents() {
                // Tooltips
                if (!config.isTestMode) {
                    try {
                        var tooltips = document.querySelectorAll('[data-bs-toggle="tooltip"]');
                        [...tooltips].forEach(el => new bootstrap.Tooltip(el));
                    } catch (e) {
                        console.log('Tooltips no disponibles:', e.message);
                    }
                }

                // Toast
                try {
                    var toastEl = document.getElementById('liveToast');
                    if (toastEl) window.appToast = new bootstrap.Toast(toastEl);
                } catch (e) {}

                // Tabs
                try {
                    const tabTriggers = document.querySelectorAll('#langTabs button[data-bs-toggle="tab"]');
                    tabTriggers.forEach(trigger => {
                        trigger.addEventListener('shown.bs.tab', () => {
                            // Asegurar que la vista previa de imagen se redimensione si es necesario
                        });
                    });
                } catch (e) {}
            },

            setupCharacterCounters() {
                const fields = [
                    { name: 'Nombre', counterId: 'nombreCounter', max: 50 },
                    { name: 'Descripcion', counterId: 'descripcionCounter', max: 500 },
                    { name: 'NombreIngles', counterId: 'nombreInglesCounter', max: 50 },
                    { name: 'DescripcionIngles', counterId: 'descripcionInglesCounter', max: 500 },
                    { name: 'NombrePortugues', counterId: 'nombrePortuguesCounter', max: 50 },
                    { name: 'DescripcionPortugues', counterId: 'descripcionPortuguesCounter', max: 500 }
                ];

                fields.forEach(field => {
                    const input = document.querySelector(`[name="${field.name}"]`);
                    const counter = document.getElementById(field.counterId);

                    if (input && counter) {
                        const update = () => {
                            const length = input.value.length;
                            counter.textContent = `${length}/${field.max}`;

                            if (!config.isTestMode) {
                                counter.className = 'text-muted';
                                if (length > field.max * 0.9) counter.classList.add('text-warning');
                                if (length > field.max) counter.classList.add('text-danger');
                            }
                        };

                        input.addEventListener('input', update);
                        update(); // Estado inicial
                    }
                });
            },

            setupImagePreview() {
                const imageUrlInput = document.getElementById('imageUrlInput');
                const imagePreview = document.getElementById('imagePreview');

                if (!imageUrlInput || !imagePreview) return;

                // Configurar validación de URL con debouncing
                let validationTimeout;
                imageUrlInput.addEventListener('input', function() {
                    clearTimeout(validationTimeout);

                    const url = this.value.trim();
                    const originalUrl = config.originalData.ImagenUrl || '';

                    // Actualizar badge si la URL cambió
                    const originalBadge = document.getElementById('originalImageBadge');
                    if (url !== originalUrl) {
                        formManager.imageChanged = true;
                        if (originalBadge) originalBadge.style.display = 'inline-block';
                    } else {
                        formManager.imageChanged = false;
                        if (originalBadge) originalBadge.style.display = 'none';
                    }

                    if (!url) {
                        resetImagePreview();
                        return;
                    }

                    validationTimeout = setTimeout(() => {
                        validateImageUrl(url, imagePreview, this);
                    }, 800);
                });

                // Inicializar con la imagen del modelo
                if (config.originalData.ImagenUrl) {
                    imagePreview.src = config.originalData.ImagenUrl;
                    imageUrlInput.classList.add('is-valid');
                }
            },

            setupChangeDetection() {
                const form = document.getElementById('typeForm');
                if (!form) return;

                const inputs = form.querySelectorAll('input, textarea');

                inputs.forEach(input => {
                    input.addEventListener('input', () => this.checkForChanges());
                    input.addEventListener('change', () => this.checkForChanges());
                });
            },

            checkForChanges() {
                let hasChanges = false;
                const form = document.getElementById('typeForm');

                if (!form) return false;

                // Verificar campos del formulario
                const inputs = form.querySelectorAll('input, textarea');
                for (let input of inputs) {
                    const name = input.name;
                    if (name && config.originalData[name] !== input.value) {
                        hasChanges = true;
                        break;
                    }
                }

                // Verificar imagen
                if (this.imageChanged) {
                    hasChanges = true;
                }

                this.hasChanges = hasChanges;
                return hasChanges;
            },

            setupUserFriendlyForm() {
                const form = document.getElementById('typeForm');
                const submitBtn = document.getElementById('submitBtn');

                if (!form || !submitBtn) return;

                // Configurar auto-capitalización
                this.setupAutoCapitalization();

                // Configurar validación en tiempo real
                this.setupRealTimeValidation(form);

                // Configurar envío del formulario
                form.addEventListener('submit', (e) => {
                    if (!this.validateFormBeforeSubmit()) {
                        e.preventDefault();
                        return false;
                    }

                    // Mostrar estado de carga
                    submitBtn.disabled = true;
                    submitBtn.innerHTML = `
                        <span class="spinner-border spinner-border-sm me-2" role="status" aria-hidden="true"></span>
                        Actualizando...
                    `;

                    return true;
                });
            },

            setupTestFriendlyForm() {
                const form = document.getElementById('typeForm');
                const submitBtn = document.getElementById('submitBtn');

                if (!form || !submitBtn) return;

                console.log('Configurando formulario para pruebas...');

                // Desactivar validaciones automáticas
                form.noValidate = true;
                form.classList.remove('needs-validation');

                // Simplificar envío del formulario
                form.addEventListener('submit', (e) => {
                    // Validación mínima
                    const nombreInput = document.querySelector('[name="Nombre"]');
                    if (!nombreInput || !nombreInput.value.trim()) {
                        e.preventDefault();
                        if (nombreInput) {
                            nombreInput.classList.add('is-invalid');
                            nombreInput.focus();
                        }
                        return false;
                    }

                    // Estado de carga simplificado
                    submitBtn.disabled = true;
                    submitBtn.innerHTML = 'Enviando...';

                    return true;
                });

                // Configurar imagen preview simple
                const imagePreview = document.getElementById('imagePreview');
                const imageUrlInput = document.getElementById('imageUrlInput');

                if (imagePreview) {
                    imagePreview.onerror = function() {
                        this.src = config.errorImage;
                    };
                }

                if (imageUrlInput) {
                    imageUrlInput.addEventListener('input', function() {
                        if (imagePreview && this.value.trim()) {
                            imagePreview.src = this.value;
                        }
                    });
                }
            },

            setupRealTimeValidation(form) {
                form.querySelectorAll('input, textarea').forEach(field => {
                    field.addEventListener('blur', function() {
                        validateSingleField(this);
                    });
                });
            },

            setupAutoCapitalization() {
                const nameFields = ['Nombre', 'NombreIngles', 'NombrePortugues'];

                nameFields.forEach(fieldName => {
                    const field = document.querySelector(`[name="${fieldName}"]`);
                    if (field) {
                        field.addEventListener('blur', function() {
                            if (this.value.trim()) {
                                this.value = this.value.trim()
                                    .toLowerCase()
                                    .replace(/\b\w/g, l => l.toUpperCase());
                            }
                        });
                    }
                });
            },

            validateFormBeforeSubmit() {
                let isValid = true;
                const form = document.getElementById('typeForm');

                // Validar campo Nombre (requerido)
                const nombreInput = form.querySelector('[name="Nombre"]');
                if (!nombreInput || !nombreInput.value.trim()) {
                    isValid = false;
                    nombreInput.classList.add('is-invalid');
                    this.showMessage('El nombre en español es requerido', 'danger');
                    nombreInput.focus();
                }

                return isValid;
            },

            setupRestoreModal() {
                const confirmBtn = document.getElementById('confirmRestore');
                const modal = document.getElementById('restoreModal');

                if (confirmBtn && modal) {
                    confirmBtn.addEventListener('click', () => {
                        const bsModal = bootstrap.Modal.getInstance(modal);
                        this.restoreForm();
                        bsModal.hide();
                        this.showMessage('Valores originales restaurados', 'success');
                    });
                }
            },

            restoreForm() {
                if (!this.checkForChanges() && !this.imageChanged) {
                    this.showMessage('No hay cambios para restaurar', 'info');
                    return;
                }

                // Restaurar valores del formulario
                for (const [name, value] of Object.entries(config.originalData)) {
                    const element = document.querySelector(`[name="${name}"]`);
                    if (element) {
                        element.value = value;
                    }
                }

                // Restaurar imagen
                const imageUrlInput = document.getElementById('imageUrlInput');
                const imagePreview = document.getElementById('imagePreview');

                if (imageUrlInput) imageUrlInput.value = config.originalData.ImagenUrl || '';
                if (imagePreview) {
                    if (config.originalData.ImagenUrl) {
                        imagePreview.src = config.originalData.ImagenUrl;
                        imageUrlInput.classList.add('is-valid');
                        imageUrlInput.classList.remove('is-invalid');
                    } else {
                        imagePreview.src = config.defaultImage;
                        imageUrlInput.classList.remove('is-valid', 'is-invalid');
                    }
                }

                // Ocultar badge de imagen original
                const originalBadge = document.getElementById('originalImageBadge');
                if (originalBadge) originalBadge.style.display = 'none';
                this.imageChanged = false;

                // Restaurar validaciones
                const form = document.getElementById('typeForm');
                if (form) {
                    form.querySelectorAll('.is-valid, .is-invalid').forEach(el => {
                        el.classList.remove('is-valid', 'is-invalid');
                    });
                }

                // Restaurar estado del botón
                const submitBtn = document.getElementById('submitBtn');
                if (submitBtn) {
                    submitBtn.disabled = false;
                    submitBtn.innerHTML = '<i class="fas fa-save me-2"></i>Actualizar Tipo';
                }

                // Actualizar contadores
                this.setupCharacterCounters();

                this.hasChanges = false;
            },

            showMessage(message, type = 'info') {
                const toastEl = document.getElementById('liveToast');
                const toastMsg = document.getElementById('toastMessage');

                if (toastEl && toastMsg && window.appToast) {
                    const iconMap = {
                        'success': 'check-circle',
                        'danger': 'exclamation-triangle',
                        'warning': 'exclamation-circle',
                        'info': 'info-circle'
                    };

                    const icon = toastEl.querySelector('i');
                    if (icon) {
                        icon.className = `fas fa-${iconMap[type] || 'info-circle'} me-2 text-${type}`;
                    }

                    toastMsg.textContent = message;
                    window.appToast.show();
                } else {
                    console.log(`${type.toUpperCase()}: ${message}`);
                }
            }
        };

        // Funciones de ayuda para la imagen
        function validateImageUrl(url, previewElement, inputElement) {
            // Validación básica de URL
            try {
                new URL(url);
            } catch {
                showImageError('URL no válida', previewElement, inputElement);
                return;
            }

            // Intentar cargar la imagen
            const img = new Image();
            img.onload = function() {
                previewElement.src = url;
                inputElement.classList.add('is-valid');
                inputElement.classList.remove('is-invalid');
                updateImageStatus('Imagen cargada', 'success');
            };

            img.onerror = function() {
                showImageError('No se pudo cargar la imagen', previewElement, inputElement);
                updateImageStatus('Error al cargar la imagen', 'danger');
            };

            img.src = url;
            updateImageStatus('Cargando imagen...', 'info');
        }

        function showImageError(message, previewElement, inputElement) {
            previewElement.src = config.errorImage;
            inputElement.classList.add('is-invalid');
            inputElement.classList.remove('is-valid');
        }

        function resetImagePreview() {
            const preview = document.getElementById('imagePreview');
            const input = document.getElementById('imageUrlInput');

            if (preview) preview.src = config.defaultImage;
            if (input) {
                input.classList.remove('is-valid', 'is-invalid');
            }
            updateImageStatus('Esperando URL de imagen', 'info');
        }

        function updateImageStatus(message, type = 'info') {
            const statusBadge = document.getElementById('imageStatus');
            if (!statusBadge) return;

            const iconMap = {
                'success': 'check-circle',
                'danger': 'exclamation-triangle',
                'warning': 'exclamation-circle',
                'info': 'info-circle'
            };

            statusBadge.innerHTML = `<i class="fas fa-${iconMap[type] || 'info-circle'} me-1"></i>${message}`;
            statusBadge.className = `badge bg-${type} px-3 py-2`;
        }

        function validateSingleField(field) {
            if (field.hasAttribute('required') && !field.value.trim()) {
                field.classList.add('is-invalid');
                field.classList.remove('is-valid');
                return false;
            }

            if (field.hasAttribute('maxlength')) {
                const max = parseInt(field.getAttribute('maxlength'));
                if (field.value.length > max) {
                    field.classList.add('is-invalid');
                    field.classList.remove('is-valid');
                    return false;
                }
            }

            if (field.value.trim()) {
                field.classList.add('is-valid');
                field.classList.remove('is-invalid');
            } else {
                field.classList.remove('is-valid', 'is-invalid');
            }

            return true;
        }

        // Inicialización principal
        document.addEventListener('DOMContentLoaded', function() {
            console.log('Inicializando formulario de edición de tipo...');

            // Inicializar gestor del formulario
            formManager.init();
        });
    </script>
}